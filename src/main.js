import "./main.css";

// NÃO renderiza template do Vite.
// Seu HTML real já está no index.html.
// Aqui só fica a inicialização do JS do seu site (se tiver).
// ✅ Isso vai impedir o Vite de sobrescrever seu site.

import "./js/main.js";